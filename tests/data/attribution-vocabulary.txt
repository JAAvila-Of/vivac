# The attributions a commit message may not carry.
#
# This is the one file in the repository that names an assistant, and it
# names them in order to refuse them. The exception is the same one
# `spanish-vocabulary.txt` carries: that file is full of Spanish in a
# project whose rule is English, because it is the fixture that enforces
# the rule, and the banned thing belongs in the list of banned things.
#
# **These are attributions, not names.** The bare name is not banned and
# cannot be: `vivac hooks` has to print `.claude/settings.json` or the
# hooks cannot be installed, and a commit explaining that hook has to be
# able to say what it hooks into. What is banned is signing the work as
# something an assistant authored -- a trailer, a generated-by footer, the
# robot. Naming a tool you integrate with is not a signature.
#
# One string per line; `#` opens a comment. Matching is case-insensitive
# against the whole message with its whitespace collapsed, so a trailer
# split across two spaces is still caught.
#
# Add to this list rather than to the script: the script has no names in
# it and is meant to keep it that way.

co-authored-by: claude
co-authored-by: chatgpt
co-authored-by: copilot
co-authored-by: codex
co-authored-by: gemini
generated with
generated by claude
generated by chatgpt
generated by copilot
generated by codex
generated by gemini
written by claude
written by chatgpt
written by copilot
written by codex
written by gemini
🤖
